;;; early-init --- code before initialization -*- lexical-binding: t; -*-
;;; Commentary:
;;; Code:

(setenv "LSP_USE_PLISTS" "true")
(setq package-enable-at-startup nil)

(provide 'early-init)
;;; early-init.el ends here
